<head>
    <title>Log-In</title>
</head>

<%- include("../partials/newSession.ejs") %>

    <body>
        <div class="container">
            <% if (currentUser) { %>
                <h1>Welcome <%= currentUser.username %>
                </h1>
                <div class="card">
                    <p>
                        You are currently logged in to your account, click the button below to log out.
                    </p>
                    <form action="/sessions?_method=DELETE" method="POST">
                        <input type="submit" class="form" value="LOG-OUT" class="btn-small red" />
                    </form>
                </div>
                <footer>
                    <%- include("../partials/footer.ejs") %>
                </footer>
        </div>
        <% } else { %>
            <div class="container">
                <h1>User Log In</h1>
                <div class="card">
                    <form action="/sessions" method="POST">
                        <fieldset>
                            <label for="username">Username:</label>
                            <input type="text" name="username" placeholder="Username...">
                        </fieldset>
                        <fieldset>
                            <label for="password">Password:</label> <br>
                            <input type="password" name="password" placeholder="Password...">
                        </fieldset>
                        <input type="submit" class="form" value="LOG-IN">
                    </form>
                </div>
                <footer>
                    <%- include("../partials/footer.ejs") %>
                </footer>
            </div>
            <% } %>
    </body>

    </html>
<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("../partials/head.ejs") %>
        <title>Exercise</title>
</head>

<body>
    <div class="container">
        <ul class="navigation">
            <%- include("../partials/listExercise.ejs") %>
        </ul>
        <h1 class="heading">
            <%=exercise.name%>
        </h1>
        <div>
            <div>
                <h3>
                    Muscle Worked: <%=exercise.muscle%>
                </h3> <br>
                <h3>
                    Difficulty Level: <%=exercise.difficulty%>/10
                </h3><br>
                <p>
                    <%=exercise.description%>
                </p><br>
                <img class="img" src="<%=exercise.image%>">
            </div>
            <% if (currentUser) { %>
                <% if (exercise.saved < 1) { %>
                    <a class="button saved" href="/saved/exercise/<%=exercise._id%>">SAVE</a> <br />
                    <%} else if (exercise.saved > 0) {%>
                        <a class="button unsaved" href="/saved/unsave/exercise/<%=exercise._id%>">UNSAVE</a> <br />
                        <% } %>
                            <a class="button edit" href="/exercises/<%=exercise._id%>/edit">EDIT</a> <br>
                            <% } %>
                                <a class="button back margin" href="/exercises">BACK</a>
        </div>
        <footer>
            <%- include("../partials/footer.ejs") %>
        </footer>
    </div>
</body>

</html>